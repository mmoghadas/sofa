<%= form_for(@watchdog) do |f| %>
  <% if @watchdog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@watchdog.errors.count, "error") %> prohibited this watchdog from being saved:</h2>

      <ul>
      <% @watchdog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
